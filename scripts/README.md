# scripts/

Placeholder for local developer tooling (e.g. lint/format wrappers, migration
runners) added in later phases. Phase 1.1 needs no scripts of its own — every
command it requires is a plain `go` or `docker compose` invocation, documented
in [`docs/development.md`](../docs/development.md).
